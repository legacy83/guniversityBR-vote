apiVersion: v1
kind: Service
metadata:
  name: result
  namespace: vote
  labels:
    app: result
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      name: "result-service"
  selector:
    app: result
    